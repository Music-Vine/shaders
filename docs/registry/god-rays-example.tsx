import { GodRays, GodRaysProps } from '@music-vine/shaders-react';

export function GodRaysExample(props: GodRaysProps) {
  return <GodRays style={{ position: 'fixed', width: '100%', height: '100%' }} {...props} />;
}
